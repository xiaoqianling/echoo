# 推送消息链路文档

## 概述

本文档详细描述了从HTTP请求开始，到消息推送到前端的完整链路。系统采用前后端分离架构，使用WebSocket实现实时消息推送。

## 整体架构

```
前端 (SolidJS + TypeScript)    后端 (NestJS + TypeScript)
     ↓                                ↓
HTTP请求 → 消息控制器 → 消息服务 → WebSocket网关 → 前端WebSocket客户端
```

## 详细链路分析

### 1. HTTP请求发起 (前端)

**文件位置**: `client/src/apps/echoo/pages/PushTestPage.tsx`

当用户在推送测试页面点击"发送消息"按钮时：

```typescript
const handleSubmit = async (e: Event) => {
  e.preventDefault();
  
  // 发送HTTP请求
  const res = await apiService.sendMessage({
    title: currentForm.title,
    desp: currentForm.desp,
    tags: currentForm.tags.length > 0 ? currentForm.tags : undefined,
    short: currentForm.short || undefined,
  });
};
```

**关键点**:
- 使用API服务发送HTTP POST请求
- 请求包含消息标题、内容、标签和简短描述
- 请求经过JWT认证中间件

### 2. API服务处理 (前端)

**文件位置**: `client/src/shared/services/api.ts`

API服务将请求转发到后端：

```typescript
async sendMessage(data: SendMessageData): Promise<Message> {
  const response = await this.request<Message>('/messages', {
    method: 'POST',
    body: JSON.stringify(data),
  });
  return response;
}
```

**关键点**:
- 自动处理JWT令牌认证
- 处理请求/响应序列化
- 统一错误处理

### 3. 消息控制器接收请求 (后端)

**文件位置**: `server/src/messages/messages.controller.ts`

后端控制器接收HTTP请求：

```typescript
@Post()
@UseGuards(JwtAuthGuard)
async sendMessage(
  @Req() req: Request,
  @Body() validatedData: SendMessageDto,
): Promise<Message> {
  return this.messagesService.sendMessage(req.user, validatedData);
}
```

**关键点**:
- 使用JWT认证守卫保护接口
- 数据验证和转换
- 调用消息服务处理业务逻辑

### 4. 消息服务处理业务逻辑 (后端)

**文件位置**: `server/src/messages/messages.service.ts`

消息服务处理消息创建和推送逻辑：

```typescript
async sendMessage(user: User, sendMessageDto: SendMessageDto): Promise<Message> {
  // 1. 创建消息实体
  const message = this.messagesRepository.create({
    ...sendMessageDto,
    sender: user,
    organization,
  });

  // 2. 保存到数据库
  const savedMessage = await this.messagesRepository.save(message);

  // 3. 准备推送通知
  const notification = {
    id: savedMessage.id,
    title: savedMessage.title,
    desp: savedMessage.desp,
    short: savedMessage.short,
    tags: savedMessage.tags,
    sender: { id: savedMessage.sender.id },
    organization: savedMessage.organization ? {
      id: savedMessage.organization.id,
    } : undefined,
    createdAt: savedMessage.createdAt,
  };
  
  // 4. 触发WebSocket推送
  await this.webSocketGateway.sendNewMessageNotification(notification);

  return savedMessage;
}
```

**关键点**:
- 消息持久化到数据库
- 根据组织/用户维度准备推送
- 调用WebSocket网关进行实时推送

### 5. WebSocket网关处理推送 (后端)

**文件位置**: `server/src/websocket/websocket.gateway.ts`

WebSocket网关负责消息分发：

```typescript
async sendNewMessageNotification(message: MessageNotification): Promise<void> {
  if (message.organization) {
    // 发送给组织内所有在线用户
    await this.queueMessage('message:new', message, 'organization', 
      [message.organization.id], 1);
  } else {
    // 发送给特定用户
    await this.queueMessage('message:new', message, 'user', 
      [message.sender.id], 1);
  }
}
```

**消息队列处理流程**:

1. **消息入队**: 将消息添加到消息队列
2. **优先级排序**: 按优先级排序消息
3. **批量处理**: 按批次处理消息队列
4. **目标分组**: 按用户/组织维度分组消息
5. **消息发送**: 发送给对应的WebSocket连接

### 6. WebSocket连接管理 (后端)

**文件位置**: `server/src/websocket/websocket.gateway.ts`

连接管理采用双向映射：

```typescript
// 用户ID到SocketID的映射
private userIdToSocketId = new Map<string, string>();

// SocketID到用户ID的映射  
private socketIdToUserId = new Map<string, string>();

// 连接信息存储
private connections = new Map<string, ConnectionInfo>();
```

**连接建立流程**:
1. 客户端连接时携带JWT令牌
2. 服务器验证令牌有效性
3. 建立用户-Socket映射关系
4. 启动心跳检测机制

### 7. 消息发送到特定用户 (后端)

**文件位置**: `server/src/websocket/websocket.gateway.ts`

```typescript
private async batchSendToUsers(messages, event: string): Promise<void> {
  for (const message of messages) {
    for (const userId of message.targetIds) {
      if (this.userIdToSocketId.has(userId)) {
        const socketId = this.userIdToSocketId.get(userId);
        if (socketId) {
          const socket = this.connections.get(socketId)?.socket;
          if (socket) {
            // 发送单个消息
            socket.emit(event, message.data);
            console.log(`📤 Sent message to user ${userId} (socket: ${socketId})`);
          }
        }
      }
    }
  }
}
```

**关键优化**:
- 直接发送单个消息而非批量消息
- 避免消息格式不匹配问题
- 添加详细日志记录

### 8. WebSocket客户端接收消息 (前端)

**文件位置**: `client/src/shared/services/websocket.ts`

前端WebSocket服务监听消息事件：

```typescript
// 新消息事件
this.socket.on("message:new", (message: Message) => {
  this.handleMessage("message:new", message);
});

// 处理消息
private handleMessage(event: string, message: Message): void {
  const callbacks = this.messageCallbacks.get(event) || [];
  callbacks.forEach((callback) => callback(message));
}
```

**消息处理流程**:
1. 监听`message:new`事件
2. 调用注册的回调函数
3. 将消息传递给通知系统

### 9. 通知系统处理 (前端)

**文件位置**: `client/src/shared/stores/notificationsStore.ts`

通知存储管理消息状态：

```typescript
export const addNotification = (message: Message): void => {
  const newNotification: Notification = {
    id: generateNotificationId(),
    message,
    read: false,
    createdAt: new Date().toISOString(),
  };

  setNotificationsState((prev) => ({
    notifications: [newNotification, ...prev.notifications],
    unreadCount: prev.unreadCount + 1,
  }));
};
```

### 10. 界面显示通知 (前端)

**文件位置**: `client/src/shared/components/BannerNotification.tsx`

BannerNotification组件显示推送消息：

```typescript
export const ToastNotification = (props: ToastNotificationProps) => {
  return (
    <div class="bg-white text-gray-800 shadow-lg rounded-lg overflow-hidden">
      <div class="p-4">
        <h3 class="font-semibold text-base">New Message</h3>
        <p class="text-gray-600 mt-1">{props.message.title}</p>
        {props.message.short && (
          <p class="text-sm text-gray-500 mt-1">{props.message.short}</p>
        )}
      </div>
    </div>
  );
};
```

## 性能优化措施

### 1. 连接管理优化
- **连接复用**: 同一用户的新连接会清理旧连接
- **心跳检测**: 30秒心跳间隔，120秒超时检测
- **全局定时器**: 使用单个全局定时器而非每个连接独立定时器

### 2. 消息发送优化
- **消息队列**: 批量处理消息，减少IO操作
- **优先级排序**: 高优先级消息优先发送
- **连接检查**: 发送前检查连接有效性

### 3. 错误处理优化
- **自动重连**: 指数退避重连机制
- **连接状态管理**: 清晰的连接状态跟踪
- **详细日志**: 完整的调试信息记录

## 故障排查指南

### 常见问题及解决方案

1. **消息未收到**
   - 检查WebSocket连接状态
   - 验证JWT令牌有效性
   - 查看服务器端消息发送日志

2. **连接频繁断开**
   - 检查心跳检测配置
   - 验证网络连接稳定性
   - 查看服务器资源使用情况

3. **消息格式错误**
   - 验证前后端消息类型定义一致性
   - 检查数据序列化/反序列化过程

## 监控指标

### 关键监控点
1. **连接数**: 当前活跃WebSocket连接数量
2. **消息队列大小**: 待发送消息数量
3. **消息发送成功率**: 成功推送到客户端的比例
4. **连接稳定性**: 平均连接时长和断开频率

## 总结

推送消息链路是一个复杂的系统，涉及HTTP请求处理、数据库操作、WebSocket通信、前端状态管理等多个环节。通过优化连接管理、消息队列处理和错误恢复机制，系统能够稳定可靠地实现实时消息推送功能。

关键的成功因素包括：
- 清晰的消息格式定义
- 可靠的连接管理机制
- 完善的错误处理策略
- 详细的日志记录系统