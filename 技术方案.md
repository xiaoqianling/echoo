# Echoo 自动推送系统技术方案

## 1. 系统架构

### 1.1 整体架构

```
┌───────────────────────────────────────────────────────────────────────┐
│                              客户端层                                 │
├─────────┬──────────┬──────────────┬───────────────────────────────────┤
│ Desktop │   Web    │    Mobile    │           第三方服务              │
│ 应用    │ 应用     │ 应用         │ （如GitHub Actions, CI/CD等）     │
└─────────┴──────────┴──────────────┴───────────────────────────────────┘
                                    │
                                    ▼
┌───────────────────────────────────────────────────────────────────────┐
│                              服务层                                  │
├─────────┬──────────┬──────────────┬───────────────────────────────────┤
│  API    │  Auth    │  Message     │            WebSocket             │
│ Gateway │ Service  │  Service     │   Server (实时消息推送)           │
└─────────┴──────────┴──────────────┴───────────────────────────────────┘
                                    │
                                    ▼
┌───────────────────────────────────────────────────────────────────────┐
│                              数据层                                  │
├─────────┬──────────┬──────────────┬───────────────────────────────────┤
│  User   │  Org     │  Message     │             Token                 │
│  DB     │  DB      │  DB          │            DB                     │
└─────────┴──────────┴──────────────┴───────────────────────────────────┘
```

### 1.2 核心流程

1. **消息发送流程**：
   - 第三方服务调用 `/api/send` 接口
   - 服务端鉴权验证
   - 消息存储到数据库
   - WebSocket 服务推送给所有在线客户端
   - 离线客户端下次登录时拉取历史消息

2. **客户端接收流程**：
   - 客户端登录获取 Token
   - 建立 WebSocket 连接
   - 接收实时消息推送
   - 支持历史消息查询

## 2. 技术栈选择

### 2.1 服务端

| 技术/框架 | 版本 | 用途 | 选型理由 |
|----------|------|------|----------|
| NestJS | ^10.x | 主框架 | 企业级Node.js框架，TypeScript优先，模块化设计，生态良好，适合未来拓展 |
| Express | ^4.x | HTTP服务器（NestJS内置） | 成熟稳定，高性能 |
| Socket.IO | ^4.x | WebSocket服务 | 支持实时双向通信，跨平台，支持多种传输方式 |
| TypeORM | ^0.3.x | ORM框架 | TypeScript友好，支持多种数据库，便于数据库迁移和管理 |
| PostgreSQL | ^15.x | 主数据库 | 关系型数据库，支持JSON类型，适合存储复杂数据结构，事务支持完善 |
| Redis | ^7.x | 缓存、会话管理 | 高性能键值存储，适合存储Token、会话信息和实时计数器 |
| JWT | - | 身份认证 | 无状态认证，便于水平扩展 |
| bcrypt | - | 密码加密 | 安全可靠的密码哈希算法 |

### 2.2 前端

| 技术/框架 | 版本 | 用途 | 选型理由 |
|----------|------|------|----------|
| SolidJS | ^1.8.x | 主框架 | 高性能，TypeScript优先，编译时优化，轻量级 |
| TypeScript | ^5.x | 开发语言 | 类型安全，提高代码质量和可维护性 |
| Vite | ^5.x | 构建工具 | 快速的开发体验，现代化的构建流程 |
| Wouter | ^2.x | 路由管理 | 轻量级路由库，适合SolidJS |
| TailwindCSS | ^3.x | CSS框架 | 实用优先，轻量级，便于定制 |
| Socket.IO Client | ^4.x | WebSocket客户端 | 与服务端Socket.IO兼容，支持实时通信 |

### 2.3 开发工具

| 工具 | 用途 |
|------|------|
| pnpm | 包管理 |
| Turborepo | 构建系统（可选，用于monorepo） |
| ESLint | 代码检查 |
| Prettier | 代码格式化 |
| Jest | 单元测试 |
| Supertest | API测试 |
| Cypress | E2E测试 |

## 3. 服务端设计

### 3.1 目录结构

```
├── src/
│   ├── app.module.ts          # 根模块
│   ├── main.ts                # 入口文件
│   ├── auth/                  # 认证模块
│   │   ├── auth.module.ts
│   │   ├── auth.controller.ts
│   │   ├── auth.service.ts
│   │   ├── jwt.strategy.ts
│   │   └── dto/
│   ├── users/                 # 用户模块
│   │   ├── users.module.ts
│   │   ├── users.controller.ts
│   │   ├── users.service.ts
│   │   └── entities/
│   ├── organizations/         # 组织模块
│   │   ├── organizations.module.ts
│   │   ├── organizations.controller.ts
│   │   ├── organizations.service.ts
│   │   └── entities/
│   ├── messages/              # 消息模块
│   │   ├── messages.module.ts
│   │   ├── messages.controller.ts
│   │   ├── messages.service.ts
│   │   ├── entities/
│   │   └── dto/
│   ├── websocket/             # WebSocket模块
│   │   ├── websocket.module.ts
│   │   ├── websocket.gateway.ts
│   │   └── websocket.service.ts
│   ├── common/                # 公共模块
│   │   ├── guards/
│   │   ├── interceptors/
│   │   ├── filters/
│   │   └── decorators/
│   └── config/                # 配置
│       └── configuration.ts
├── test/                      # 测试目录
├── nest-cli.json              # NestJS配置
├── tsconfig.json              # TypeScript配置
├── tsconfig.build.json        # TypeScript构建配置
└── package.json               # 依赖配置
```

### 3.2 核心模块设计

#### 3.2.1 认证模块 (Auth)

- **功能**：处理用户登录、注册、Token生成与验证
- **核心API**：
  - `POST /auth/register` - 用户注册
  - `POST /auth/login` - 用户登录
  - `GET /auth/refresh` - 刷新Token

#### 3.2.2 用户模块 (Users)

- **功能**：用户信息管理
- **核心API**：
  - `GET /users/me` - 获取当前用户信息
  - `PUT /users/me` - 更新用户信息
  - `GET /users/:id` - 获取指定用户信息

#### 3.2.3 组织模块 (Organizations)

- **功能**：组织管理、成员管理
- **核心API**：
  - `POST /organizations` - 创建组织
  - `GET /organizations` - 获取组织列表
  - `GET /organizations/:id` - 获取组织详情
  - `POST /organizations/:id/members` - 添加成员
  - `DELETE /organizations/:id/members/:userId` - 移除成员

#### 3.2.4 消息模块 (Messages)

- **功能**：消息发送、存储、查询
- **核心API**：
  - `POST /api/send` - 发送消息（对外公开接口）
  - `GET /messages` - 获取消息列表
  - `GET /messages/:id` - 获取消息详情

#### 3.2.5 WebSocket模块 (WebSocket)

- **功能**：实时消息推送
- **核心事件**：
  - `connection` - 客户端连接
  - `disconnect` - 客户端断开连接
  - `message:new` - 新消息推送
  - `user:online` - 用户上线通知
  - `user:offline` - 用户离线通知

### 3.3 数据库设计

#### 3.3.1 用户表 (users)

| 字段名 | 类型 | 约束 | 描述 |
|--------|------|------|------|
| id | UUID | PRIMARY KEY | 用户ID |
| email | VARCHAR(255) | UNIQUE NOT NULL | 邮箱 |
| password | VARCHAR(255) | NOT NULL | 密码哈希 |
| name | VARCHAR(100) | NOT NULL | 用户名 |
| avatar | VARCHAR(255) | NULL | 头像URL |
| created_at | TIMESTAMP | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL DEFAULT CURRENT_TIMESTAMP | 更新时间 |

#### 3.3.2 组织表 (organizations)

| 字段名 | 类型 | 约束 | 描述 |
|--------|------|------|------|
| id | UUID | PRIMARY KEY | 组织ID |
| name | VARCHAR(100) | NOT NULL | 组织名称 |
| description | TEXT | NULL | 组织描述 |
| owner_id | UUID | NOT NULL REFERENCES users(id) | 拥有者ID |
| created_at | TIMESTAMP | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL DEFAULT CURRENT_TIMESTAMP | 更新时间 |

#### 3.3.3 组织成员表 (organization_members)

| 字段名 | 类型 | 约束 | 描述 |
|--------|------|------|------|
| id | UUID | PRIMARY KEY | 成员ID |
| organization_id | UUID | NOT NULL REFERENCES organizations(id) | 组织ID |
| user_id | UUID | NOT NULL REFERENCES users(id) | 用户ID |
| role | VARCHAR(20) | NOT NULL DEFAULT 'member' | 角色（owner, admin, member） |
| joined_at | TIMESTAMP | NOT NULL DEFAULT CURRENT_TIMESTAMP | 加入时间 |

#### 3.3.4 消息表 (messages)

| 字段名 | 类型 | 约束 | 描述 |
|--------|------|------|------|
| id | UUID | PRIMARY KEY | 消息ID |
| title | VARCHAR(255) | NOT NULL | 标题 |
| desp | TEXT | NULL | 消息正文（Markdown） |
| short | VARCHAR(255) | NULL | 简短描述 |
| tags | JSONB | NULL | 标签数组 |
| sender_id | UUID | NOT NULL REFERENCES users(id) | 发送者ID |
| organization_id | UUID | NULL REFERENCES organizations(id) | 所属组织ID |
| created_at | TIMESTAMP | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL DEFAULT CURRENT_TIMESTAMP | 更新时间 |

#### 3.3.5 客户端表 (clients)

| 字段名 | 类型 | 约束 | 描述 |
|--------|------|------|------|
| id | UUID | PRIMARY KEY | 客户端ID |
| user_id | UUID | NOT NULL REFERENCES users(id) | 用户ID |
| type | VARCHAR(20) | NOT NULL | 客户端类型（web, desktop, mobile） |
| device_info | JSONB | NULL | 设备信息 |
| last_online | TIMESTAMP | NULL | 最后在线时间 |
| created_at | TIMESTAMP | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |

### 3.4 鉴权设计

1. **API鉴权**：
   - 使用 JWT Token 进行鉴权
   - Token 包含用户ID、角色、过期时间
   - 支持刷新Token机制
   - 敏感操作需要二次验证

2. **接口权限控制**：
   - 基于角色的访问控制（RBAC）
   - 支持用户级和组织级权限
   - 自定义装饰器 `@Roles()` 实现权限控制

3. **WebSocket鉴权**：
   - 连接时通过Token验证身份
   - 维护在线用户列表
   - 支持断开重连机制

### 3.5 消息推送机制

1. **实时推送**：
   - 使用 WebSocket 实现实时消息推送
   - 支持一对一、一对多、广播消息
   - 消息队列确保消息可靠送达

2. **离线消息**：
   - 消息存储到数据库
   - 客户端上线时拉取未读消息
   - 支持消息状态标记（已读、未读）

3. **消息优先级**：
   - 支持消息优先级设置
   - 高优先级消息优先推送

## 4. 前端设计

### 4.1 目录结构

```
├── src/
│   ├── App.tsx               # 根组件
│   ├── main.tsx              # 入口文件
│   ├── index.html            # HTML模板
│   ├── routes/               # 路由配置
│   │   ├── index.tsx
│   │   ├── auth/             # 认证路由
│   │   ├── dashboard/        # 仪表盘路由
│   │   └── messages/         # 消息路由
│   ├── components/           # 通用组件
│   │   ├── Layout.tsx
│   │   ├── Header.tsx
│   │   └── Sidebar.tsx
│   ├── services/             # API服务
│   │   ├── api.ts
│   │   ├── auth.ts
│   │   └── websocket.ts
│   ├── stores/               # 状态管理
│   │   └── useAuthStore.ts
│   ├── types/                # TypeScript类型
│   │   └── index.ts
│   └── styles/               # 全局样式
│       └── global.css
├── vite.config.ts            # Vite配置
├── tsconfig.json             # TypeScript配置
└── package.json              # 依赖配置
```

### 4.2 核心功能

1. **用户认证**：
   - 登录、注册、退出
   - Token管理
   - 自动登录

2. **消息中心**：
   - 实时消息接收
   - 历史消息查询
   - 消息筛选（按标签、时间）
   - 消息状态管理

3. **组织管理**：
   - 组织列表
   - 组织详情
   - 成员管理

4. **设置中心**：
   - 用户信息设置
   - 通知设置
   - 客户端管理

### 4.3 路由设计

| 路由 | 组件 | 权限 | 描述 |
|------|------|------|------|
| / | LoginPage | 公开 | 登录页 |
| /register | RegisterPage | 公开 | 注册页 |
| /dashboard | DashboardPage | 需要认证 | 仪表盘 |
| /messages | MessagesPage | 需要认证 | 消息列表 |
| /messages/:id | MessageDetailPage | 需要认证 | 消息详情 |
| /organizations | OrganizationsPage | 需要认证 | 组织列表 |
| /organizations/:id | OrganizationDetailPage | 需要认证 | 组织详情 |
| /settings | SettingsPage | 需要认证 | 设置中心 |

## 5. 部署方案

### 5.1 开发环境

- **服务端**：
  - 使用 Docker Compose 启动 PostgreSQL 和 Redis
  - `npm run start:dev` 启动开发服务器

- **前端**：
  - `npm run dev` 启动开发服务器

### 5.2 生产环境

- **服务端**：
  - 使用 Docker 容器化部署
  - Nginx 作为反向代理
  - PM2 管理 Node.js 进程
  - 配置 HTTPS

- **前端**：
  - 静态资源部署到 CDN
  - Nginx 作为静态资源服务器
  - 配置 HTTPS

- **数据库**：
  - PostgreSQL 主从复制
  - Redis 集群
  - 定期备份

### 5.3 CI/CD

- 使用 GitHub Actions 或 GitLab CI
- 自动测试
- 自动构建
- 自动部署

## 6. 安全设计

### 6.1 服务端安全

- 输入验证和参数校验
- SQL注入防护（使用TypeORM参数化查询）
- XSS防护（输出编码）
- CSRF防护（使用SameSite Cookie）
- 密码哈希存储（使用bcrypt）
- Token过期机制
- 限制API请求频率
- 敏感数据加密存储

### 6.2 前端安全

- 防止XSS攻击（使用SolidJS自动转义）
- 防止CSRF攻击（使用SameSite Cookie）
- 安全的Token存储（使用HttpOnly Cookie）
- 敏感操作二次验证
- 定期更新依赖

## 7. 监控与日志

### 7.1 服务端监控

- 使用 Prometheus 收集 metrics
- Grafana 可视化监控数据
- 监控指标：
  - API请求量和响应时间
  - WebSocket连接数
  - 数据库查询性能
  - 系统资源使用率

### 7.2 日志系统

- 使用 Winston 或 Pino 记录日志
- 日志级别：debug, info, warn, error
- 日志格式：JSON格式，便于分析
- 支持日志轮换和压缩
- 异常日志告警

## 8. 扩展性设计

### 8.1 服务端扩展性

- 模块化设计，便于添加新功能
- 支持水平扩展（无状态设计）
- 微服务架构预留（NestJS支持微服务）
- 插件机制，支持第三方集成

### 8.2 前端扩展性

- 组件化设计，便于复用
- 路由模块化，便于添加新业务
- 状态管理模块化
- 支持微前端架构（预留）

## 9. 测试策略

### 9.1 服务端测试

- 单元测试：使用 Jest 测试核心功能
- API测试：使用 Supertest 测试API接口
- 集成测试：测试模块间集成
- 端到端测试：使用 Cypress 测试完整流程

### 9.2 前端测试

- 单元测试：使用 Vitest 测试组件
- 集成测试：测试组件间集成
- 端到端测试：使用 Cypress 测试完整流程

## 10. 开发计划

### 10.1 第一阶段：基础架构搭建（2周）

- 服务端框架搭建
- 数据库设计和初始化
- 认证模块实现
- WebSocket服务实现
- 基础API接口实现

### 10.2 第二阶段：核心功能开发（2周）

- 消息发送接口实现
- 消息推送功能实现
- 前端基础架构搭建
- 用户认证功能实现
- 消息中心前端实现

### 10.3 第三阶段：完善和测试（1周）

- 组织管理功能实现
- 消息历史记录功能实现
- 测试用例编写
- 性能优化
- 安全加固

### 10.4 第四阶段：部署和文档（1周）

- 生产环境部署
- 监控和日志系统搭建
- 开发文档编写
- 用户手册编写

## 11. 总结

Echoo自动推送系统采用了现代化的技术栈和架构设计，具有以下特点：

1. **企业级架构**：使用NestJS框架，模块化设计，便于未来拓展
2. **全平台支持**：支持Web、Desktop、Mobile等多平台客户端
3. **实时通信**：基于WebSocket实现实时消息推送
4. **安全性高**：完善的鉴权和安全机制
5. **扩展性强**：预留了微服务和微前端架构，便于未来业务拓展
6. **开发效率高**：使用TypeScript全栈开发，提高代码质量和可维护性

该系统可以满足开发者通过简单的HTTP接口发送消息，然后全平台应用都能收到消息推送的需求，同时为未来业务拓展留出了空间。